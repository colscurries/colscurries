{
  "name": "colscurries",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "deploy": "cat ./assets/styles.scss | node_modules/.bin/node-sass --include-path node_modules/ --output-style compressed > assets/styles.css"
  },
  "devDependencies": {
    "bootstrap": "^4.1.3",
    "node-sass": "^4.9.4"
  }
}
